# Normalize line endings to LF on checkout everywhere — shell scripts and the Docker
# entrypoint must be LF to run on Linux.
* text=auto eol=lf

*.sh    text eol=lf
*.conf  text eol=lf
Dockerfile text eol=lf

# Binary assets
*.png binary
*.jpg binary
*.pdf binary
*.ico binary
